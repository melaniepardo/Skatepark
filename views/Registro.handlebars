<h1>Skate Park</h1>

<div class="py-4">
    <h2>Registro</h2>
    <hr class="w-50" />

    <form>
        <div id="form" class="form-group row w-50 m-auto">
            <div class="form-group col-12 col-sm-6">
                <label>Email</label>
                <input class="form-control m-auto" />
            </div>
            <div class="form-group col-12 col-sm-6">
                <label>Nombre</label>
                <input class="form-control m-auto" />
            </div>
            <div class="form-group col-12 col-sm-6">
                <label>Password</label>
                <input class="form-control m-auto" />
            </div>
            <div class="form-group col-12 col-sm-6">
                <label>Repita la password</label>
                <input class="form-control m-auto" />
            </div>
            <div class="form-group col-12 col-sm-6">
                <label>Años de experiencia</label>
                <input class="form-control m-auto" />
            </div>
            <div class="form-group col-12 col-sm-6">
                <label>Especialidad</label>
                <input class="form-control m-auto" />
            </div>
            <div id="foto" class="form-group col-12 col-sm-12">
                <label>Foto de perfil</label>
                <input type="file" />
            </div>
        </div>
        <button class="btn btn-info mb-3">Registrarme</button>
        <p><a href="#"> Iniciar sesión</a></p>
    </form>
</div>


<script>
    let url = 'http://localhost:3000/usuario';
    let email = document.getElementById('email');
    let nombre = document.getElementById('nombre');
    let password = document.getElementById('password');
    let password_2 = document.getElementById('password-2');
    let anios = document.getElementById('anios');
    let especialidad = document.getElementById('especialidad');

    document.getElementById("form").addEventListener("submit", async (event) => {
        event.preventDefault()

        email;
        nombre;
        password;
        password_2;
        anios;
        especialidad;
        let nombre_foto = document.getElementById('foto').files[0].name;

        if (password.value !== password_2.value) {
            alert('Los contraseñas no coinciden')

        } else {

            let payload = {
                email: email.value,
                nombre: nombre.value,
                password: password.value,
                anios: anios.value,
                especialidad: especialidad.value,
                nombre_foto: nombre_foto,
            }

            try {
                var form = document.getElementById('form');
                let formData = new FormData(frm);
                await axios.post(url, formData, {
                    headers: {
                        "Content-Type": `multipart/form-data; boundary=${formData._boundary}`,
                    },
                })

                    .then(() => {
                        axios.post('http://localhost:3000/subir_foto')
                    });
                alert('Usuario registrado con éxito')
                window.location.href = '/login'
            } catch (error) {
                // const { data } = response
                //const { error } = data
                //alert(error)
                console.log(error)
            }
        }
    })

</script>